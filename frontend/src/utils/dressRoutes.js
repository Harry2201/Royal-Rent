/** Canonical dress detail route (marketplace + landing cards). */
export const dressDetailPath = (id) => `/dress/${id}`;

/** Legacy path kept for redirects and API alignment. */
export const dressDetailPathLegacy = (id) => `/dresses/${id}`;
